# Test

Add or improve focused tests for the assigned behavior. Prefer public behavior and stable seams over internal implementation coupling. Change production code only when the task explicitly requires a minimal testability seam, and call that out in the handoff.
