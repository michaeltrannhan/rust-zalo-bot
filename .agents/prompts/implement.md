# Implement

Implement only the assigned behavior. Reuse existing seams and conventions, keep the diff cohesive, and add or update focused tests when behavior changes. Surface any requirement conflict instead of silently widening scope.
